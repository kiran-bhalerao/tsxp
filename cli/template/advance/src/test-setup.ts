beforeAll(async () => {
  // this will run before all tests cases
  // use this to initialization like DB connection etc.
})

beforeEach(async () => {
  // this trigger before each test case
  // cleanup all tables or all collections
})

afterAll(async () => {
  // this will run after all test cases done
  // use to disconnect services like DB connection.
})
